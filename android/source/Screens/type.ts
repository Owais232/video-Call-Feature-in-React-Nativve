// src/types.ts

export type RootStackParamList = {
    VideoCallScreen: { selectedUser: any }; // Adjust `any` to the type of `selectedUser` if you know it
    // Add other screens and their params if necessary
  };
  